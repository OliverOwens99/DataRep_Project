function view() {
    // Code to fetch games from the database and store them in a variable called 'games'

    games.forEach((game) => {
        // Code to create a card element for each game and display it on the page
    });
}


export default View;
